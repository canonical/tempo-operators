name: Pull Requests

on:
  pull_request:
    branches:
      - main
      - track/**

jobs:
  pull-request:
    name: PR
    uses: canonical/observability/.github/workflows/charm-pull-request.yaml@5127d643c26f438fe41b1068066ff281221e5d49 # v1
    secrets: inherit
